---
"@still-void/ui": patch
---

Refresh README to match the v2.0.0 shape: correct test count (31 → 320), remove the dead `npm run playground` script reference (the framework-free catalog was removed with the core entry point), and correct the note about Tailwind CSS — it's a required app dependency for shadcn/ui components, not a declared peer dependency of this package.
