// Single source of truth is src/tailwind-preset.ts — see that file for why
// the config object lives there instead of here (it's what gets published
// and typechecked as the `./tailwind-preset` export).
export { default } from './src/tailwind-preset';
